HELP_FETCH_POSTS_NEWER_FIRST = (
    '\bDefines how offset is being calculated on each iteration:\n'
    '\bTrue: the offset starts with 0 and is incremented by number of posts fetched\n'
    '\bFalse (default): the offset is always the number of posts in the DB, for given blog'
)
